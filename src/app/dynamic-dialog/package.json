{
  "name": "ngx4-dialog",
  "version": "1.0.4",
  "description": "ngx dynamic dialog",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "clean": "rimraf index.js index.js.map index.d.ts src/**/*.js src/**/*.js.map src/**/*.d.ts linktest.tgz",
    "pack": "npm run prepublish && npm pack --filename ngx-dialog.tgz"
  },
  "license": "MIT",
  "author": "sunshine",
  "keywords": ["ngx"],
  "dependencies": {
    "@angular/common": "^5.2.0",
    "@angular/core": "^5.2.0"
  },
  "devDependencies": {
    "typescript": "~2.5.3"
  }
}
