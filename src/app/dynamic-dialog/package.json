{
  "name": "ngx4-dialog",
  "version": "1.0.5",
  "description": "ngx dynamic dialog",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "clean": "rimraf index.js index.js.map index.d.ts lib/**/*.js lib/**/*.js.map lib/**/*.d.ts ngx-dialog.tgz",
    "pack": "npm run prepublish && npm pack --filename ngx-dialog.tgz"
  },
  "license": "MIT",
  "author": "sunshine",
  "keywords": ["ngx", "dialog", "modal"],
  "dependencies": {
    "@angular/common": "^5.2.0",
    "@angular/core": "^5.2.0"
  },
  "devDependencies": {
    "typescript": "~2.5.3"
  }
}
